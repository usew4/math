# 问题二适配验证记录

## 实际执行环境

- 本地 Windows，NVIDIA GeForce RTX 4060 Laptop 8GB。
- 使用已有 `tuduipytorch` 环境：PyTorch 2.5.1，CUDA运行库11.8。
- 没有连接或安装用户的Ubuntu／RTX3090服务器；服务器目标环境为Python3.10、PyTorch2.4.0/cu121，安装命令见README_Q2.md。
- 本次没有进行完整训练，没有生成附件3预测，也没有修改LNLN或赛题原始数据。

## 数据检查

附件2未对齐版train/valid/test样本数为3395/728/727。检查确认三分类标签与回归标签符号完全一致（负向0、中性1、正向2）；文本、音频、视觉特征没有NaN/Inf。视觉有效长度存在1，因此短序列跳过擦除逻辑是必要的。

## 已执行验证

1. `python EBMC/test_q2.py`：4项测试通过。覆盖七种模态组合、连续区间、文本特殊词元保护、满长度文本、无效填充、过短模态、取样顺序无关的可复现性、两阶段单样本前向/反向、冻结教师、全零特征/全屏蔽输入、无标签且无教师的推理、保存加载预测一致、随机状态恢复及恢复后下一次CPU参数更新逐位一致。
2. 真实附件2训练/验证各前4条，默认256维、4层、500位置、批量2，两阶段各1个训练批次；每阶段均完成22个固定验证场景。
3. 第一阶段训练总损失26.82320，第二阶段4058506.25000，损失及梯度有限且梯度非零。仅两次参数更新，损失量级很大，不能证明收敛；正式训练应监测各轮损失和验证曲线。这些结果不用于性能结论。
4. 第一阶段GPU峰值分配约311.25 MiB，第二阶段约885.56 MiB。此为本地批量2测量，不是3090上批量8的实测。
5. 独立进程加载 `best.pt`，以纯预测路径重新评价22个场景，与保存前全部场景指标完全一致。

本地日志与检查点保存在 `runs/q2_smoke_default/`，重载验证在 `runs/q2_smoke_reload/`，均由.gitignore排除。调试阶段另有32维1层的 `runs/q2_smoke/`。这些权重仅作链路验证，不能作为正式模型提交。

## 服务器仍需确认

执行README中的安装、单元测试及默认结构小规模验证，然后再启动批量8正式训练。确认CUDA设备可用、实际显存峰值、训练与验证损失走势。附件3的文本编码匹配及缺少有效长度的专项接口不在本次范围内。
